# restart sketchybar
alt + cmd - r : sketchybar --reload

# update homebrew packages and restart sketchybar
alt + cmd - b : brew upgrade && sketchybar --reload

# stop/start/restart yabai
ctrl + alt - r : borders order=above
